#Check remote origin -
git remote -v

#Remove remote origin -
git remote remove origin

#Set new remote origin -
git remote add origin git://new.url.here

#Merging two repos with different histories -
git pull --allow-unrelated-histories
